# Dependencias (la carpeta más pesada que NUNCA se sube)
node_modules/

# Archivos de construcción (la carpeta que se crea al hacer npm run build)
dist/
build/

# Variables de entorno (donde guardas llaves secretas de Firebase)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Archivos del sistema y logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
.vscode/
